# Environment variables for the worker service.
# Source the repository-level `.env` before using these values.

BACKEND_API_URL=http://localhost:4000
QUEUE_REDIS_URL=${REDIS_URL}
WORKER_STORAGE_DIR=${STORAGE_ROOT}/worker
