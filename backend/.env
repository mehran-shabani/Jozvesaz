# Environment variables for the backend service.
# Load the root `.env` for shared values before exporting these.

PORT=4000
DATABASE_URL=${POSTGRES_HOST}/backend
REDIS_CACHE_URL=${REDIS_URL}
FILE_STORAGE_ROOT=${STORAGE_ROOT}/backend
