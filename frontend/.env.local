# Environment variables for the frontend service.
# Source the repository-level `.env` first so shared values like STORAGE_ROOT are available.

NEXT_PUBLIC_API_BASE_URL=http://localhost:4000
NEXT_PUBLIC_STORAGE_BASE_URL=${STORAGE_ROOT}/public
