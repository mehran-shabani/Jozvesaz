# Environment variables for the PostgreSQL container.
# Use strong credentials in production and keep this file out of version control when storing secrets.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=backend
